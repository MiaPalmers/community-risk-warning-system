# Tasks
- [x] Task 1: 将 `createBrowserRouter` 替换为 `createHashRouter`
  - [x] SubTask 1.1: 修改 `src/router/index.tsx` 中的 import 语句，将 `createBrowserRouter` 改为 `createHashRouter`
  - [x] SubTask 1.2: 修改路由创建调用，将 `createBrowserRouter([...])` 改为 `createHashRouter([...])`

# Task Dependencies
- 无外部依赖，Task 1 为独立改动
